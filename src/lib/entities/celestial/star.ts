import { Size } from '../size';
import { Temperature } from '../temperature';

export class Star {
	temperature = Temperature.cold;
	luminosity = Size.tiny;
	size = Size.tiny;
	mass = Size.tiny;
	color = 'red';
}
